the primary target is to assiciate observations to "current focused window id" like clipboard read/write, selection read/write, keyboard events. also collect x11 events like window title changes, virtual desktop changes. then search by keywords, view by timeline, switch back and forth from timeline and keyword search view, then jump to the window if we can find it and it is alive, through boot_id and window_id combination. 